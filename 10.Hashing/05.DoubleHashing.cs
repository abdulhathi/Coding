using System;

/*
    h1(x) = x % 10
    h2(x) = PrimeNum * (x % PrimeNum)
    h(x) = (h1(x) + (i * h2(x)) Where i = 0,1,2,3,4....
*/
public class DoubleHashing {
    public DoubleHashing() {

    }
}